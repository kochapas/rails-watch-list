<a href="" data-toggle="modal" data-target="#exampleModalCenter-<%= @list.id %>-<%= @movie.id %>">
  <div>
    <div class="card-category card-category-bg" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= @movie.poster_url %>)">
      <%= @movie.title %>
    </div>
  </div>
</a>
<%= render 'lists/modalbox' %>
